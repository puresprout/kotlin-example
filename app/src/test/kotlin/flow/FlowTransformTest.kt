package com.purestation.app.flow

/**
 * 변환 (Transform)
 *
 * map, mapLatest (최신값만 처리)
 * flatMapConcat, flatMapMerge, flatMapLatest
 * transform (map+filter+emit 조합 가능)
 */
class FlowTransformTest {
}